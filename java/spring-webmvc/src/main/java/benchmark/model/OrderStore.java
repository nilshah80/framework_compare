package benchmark.model;

/**
 * @deprecated Replaced by benchmark.pgstore.PgStore — PostgreSQL-backed storage.
 * This class is kept as a placeholder to avoid breaking imports during transition.
 * All in-memory storage has been removed.
 */
@Deprecated
public class OrderStore {
    // Intentionally empty — all storage now handled by PgStore
}
